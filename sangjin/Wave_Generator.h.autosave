#ifndef WAVEGENERATOR_H
#define WAVEGENERATOR_H

#include "Enemy.h"
#include <vector>


class WaveGenerator
{
public:
    WaveGenerator();
    std::vector MakeSpawnList(int round);
    void ClearSpwanList();
    
signals:

public slots:
    std::vector SpawnList;
};

#endif // WAVEGENERATOR_H
